const categories = require('./categories');
const news = require('./news');
const progressController = require('./progress');
const work = require('./work');
const user = require('./user');

module.exports = {
    categories,
    news,
    progressController,
    work,
    user
};